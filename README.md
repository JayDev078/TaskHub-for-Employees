# TaskHub for Employees
 I’ve built a compact, feature-rich Employee Management System where admins can create, assign, and track tasks (Completed, Pending, Failed). Employees can view, accept, forward tasks, and update progress. The app uses local storage, making it lightweight, efficient, and easy to deploy without backend dependencies.
